# CRA build config (read by react-scripts for ALL builds, incl. the SWA deploy).
# Security: do NOT emit public source maps in production — they expose the full app
# source (a security-review finding, 2026-07-31). Local dev builds are unaffected.
GENERATE_SOURCEMAP=false
